<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.app_a_void">

    <!-- Permission to access usage stats for screen time tracking -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Permission for foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- If you truly need to see all packages, keep QUERY_ALL_PACKAGES; otherwise consider removing. -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- Foreground service type "dataSync" (for Android 14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Optional: remove if not needed -->
    <!-- <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" /> -->
    <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> -->
    <!-- <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> -->
    <!-- <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" /> -->

    <!-- Queries element to specify package visibility requirements -->
    <queries>
        <!-- Intent query to see all apps that have a launcher entry -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppaVoid"
        android:enableOnBackInvokedCallback="true"
        tools:replace="android:enableOnBackInvokedCallback">

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppaVoid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ScreenTimeStatsActivity (exported=false if only used internally) -->
        <activity
            android:name=".ScreenTimeStatsActivity"
            android:exported="false" />

        <!-- LockScreenActivity (New) -->
        <activity
            android:name=".LockScreenActivity"
            android:theme="@style/Theme.AppaVoid.LockScreen"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:screenOrientation="portrait" />

        <!-- Broadcast receiver for resetting data at midnight/time changes -->
        <receiver
            android:name=".MidnightResetReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Accessibility Service declaration with foreground service support -->
        <service
            android:name=".TimeWarpAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"  android:enabled="true"
            android:label="@string/time_warp_accessibility_description"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/time_warp_accessibility_config" />
        </service>

    </application>

</manifest>
